<nav class="navbar navbar-expand-md navbar-dark bg-dark fixed-top">
    <div class="container-fluid">
        <a class="navbar-brand" href="/">
            PlaygroundOne
        </a>

        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarContent"
                aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarContent">
            <div class="navbar-nav mr-auto">
                <a class="nav-link" href="#">#GuestMenu</a>
                {% if app.user %}
                    <a class="nav-link" href="#">#UserMenu</a>
                    {% if is_granted('ROLE_ADMIN') %}
                        <a class="nav-link" href="{{ path('admin') }}">{{ 'Admin'|trans }}</a>
                    {% endif %}
                {% endif %}
                <a class="nav-link" href="{{ path('about') }}">About</a>
            </div>
            <div class="navbar-nav ms-auto">
                <select name="" id="" onchange="window.location.href='/switch-locale/'+this.value">
                    {% for locale in locales|split(',') %}
                    <option value="{{ locale }}" {% if locale == app.request.locale %}selected="selected"{% endif %}>
                        {{ locale }}
                    </option>
                    {% endfor %}
                </select>
                {% if app.user %}
                    <div class="dropdown dropstart">
                        <button class="btn btn-secondary dropdown-toggle" type="button" id="dropdownMenuButton1"
                                data-bs-toggle="dropdown" aria-expanded="false">
                            <i class="bi-person"></i>
                        </button>
                        <ul class="dropdown-menu dropdown-menu-dark" aria-labelledby="dropdownMenuButton1">
                            <li><a class="dropdown-item" href="{{ path('app_profile') }}"><i class="bi-person"></i> Profile</a></li>
                            <li><a class="dropdown-item" href="#"><i class="bi-star"></i> Another action</a></li>
                            <li><hr class="dropdown-divider"></li>
                            <li>
                                <a class="dropdown-item" href="{{ path('logout') }}" title="{{ 'Logout'|trans }}">
                                    <i class="bi-box-arrow-up-right"></i> Logout
                                </a>
                            </li>
                        </ul>
                    </div>
                {% else %}
                    <a class="nav-link" accesskey="l" href="{{ path('login') }}" title="{{ 'Login'|trans }}">
                        <i class="bi-box-arrow-in-down-right"></i>
                    </a>
                {% endif %}
            </div>
        </div>
    </div>
</nav>
