<nav class="navbar navbar-expand-md navbar-dark bg-dark fixed-top">
    <div class="container-fluid">
        <a class="navbar-brand" href="/">
            PlaygroundOne
        </a>

        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent"
                aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav mr-auto">
                <li class="nav-item">
                    <a class="nav-link" href="#">###</a>
                </li>
                {% if app.user %}
                    <li class="nav-item">
                        <a class="nav-link" href="#">###</a>
                    </li>
                    {% if is_granted('ROLE_ADMIN') %}
                        <li class="nav-item dropdown">
                            <a class="nav-link dropdown-toggle" href="#" id="dropdown01" role="link"
                               data-bs-toggle="dropdown">
                                {{ 'Admin'|trans }}
                            </a>
                            <ul class="dropdown-menu" aria-labelledby="dropdown01">
                                <li><a class="dropdown-item" href="{{ path('user_index') }}">{{ 'Users'|trans }}</a>
                                </li>
                                <li><a class="dropdown-item" href="#">###</a></li>
                                <li>
                                    <hr class="dropdown-divider">
                                </li>
                                <li><a class="dropdown-item" href="#">###</a></li>
                            </ul>
                        </li>
                    {% endif %}
                {% endif %}
            </ul>
            <div class="ms-auto">
                {% if app.user %}
                    <a class="nav-link" accesskey="l" href="{{ path('logout') }}" title="{{ 'Logout'|trans }}">
                        <i class="bi-box-arrow-up-right"></i>
                    </a>
                {% else %}
                    <a class="nav-link" accesskey="l" href="{{ path('login') }}" title="{{ 'Login'|trans }}">
                        <i class="bi-box-arrow-in-down-right"></i>
                    </a>
                {% endif %}
            </div>
        </div>
    </div>
</nav>
