build:
    dependencies:
        override:
            - npm install -g 'yarn'
            - yarn install
            - composer install

coding_style:
    php:
        spaces:
            within:
                brackets: false
